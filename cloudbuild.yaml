steps:
  # --- Backend Build & Push ---
  - name: 'gcr.io/cloud-builders/docker'
    id: 'build-backend'
    args: ['build', '-t', '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPOSITORY}/backend:${_TAG}', './backend']

  - name: 'gcr.io/cloud-builders/docker'
    id: 'push-backend'
    args: ['push', '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPOSITORY}/backend:${_TAG}']

  # --- Build Frontend with build args ---
  - name: 'gcr.io/cloud-builders/docker'
    id: 'build-frontend'
    args:
      - 'build'
      - '--build-arg'
      - 'NEXT_PUBLIC_FIREBASE_API_KEY=${_FIREBASE_API_KEY}'
      - '--build-arg'
      - 'NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=${_FIREBASE_AUTH_DOMAIN}'
      - '--build-arg'
      - 'NEXT_PUBLIC_FIREBASE_PROJECT_ID=${PROJECT_ID}'
      - '--build-arg'
      - 'NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=${_FIREBASE_STORAGE_BUCKET}'
      - '--build-arg'
      - 'NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=${_FIREBASE_MESSAGING_SENDER_ID}'
      - '--build-arg'
      - 'NEXT_PUBLIC_FIREBASE_APP_ID=${_FIREBASE_APP_ID}'
      - '--build-arg'
      - 'NEXT_PUBLIC_BACKEND_API_URL=${_BACKEND_API_URL}'
      - '-t'
      - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPOSITORY}/frontend:${_TAG}'
      - './frontend'

  - name: 'gcr.io/cloud-builders/docker'
    id: 'push-frontend'
    args: ['push', '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPOSITORY}/frontend:${_TAG}']

  # --- Deploy Backend to Cloud Run ---
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'deploy-backend'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'land-guard-api'
      - '--image'
      - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPOSITORY}/backend:${_TAG}'
      - '--region'
      - '${_REGION}'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--set-env-vars'
      - 'GCP_PROJECT_ID=${PROJECT_ID},GCP_LOCATION=${_REGION},GCS_BUCKET_NAME=land-guard-docs-${PROJECT_ID},FIREBASE_PROJECT_ID=${PROJECT_ID},ALLOW_GUEST_AUTH=false,CORS_ORIGINS=${_FRONTEND_URL},ENABLE_DOCS=false,LOG_FORMAT=json,LOG_LEVEL=INFO,WEB_CONCURRENCY=2'
      - '--port'
      - '8000'
      - '--service-account'
      - 'sa-${PROJECT_ID}@${PROJECT_ID}.iam.gserviceaccount.com'
      - '--memory'
      - '2Gi'
      - '--cpu'
      - '4'
      - '--concurrency'
      - '10'
      - '--timeout'
      - '300'
      - '--min-instances'
      - '1'

  # --- Deploy Frontend to Cloud Run ---
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'deploy-frontend'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'land-guard-web'
      - '--image'
      - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPOSITORY}/frontend:${_TAG}'
      - '--region'
      - '${_REGION}'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--set-env-vars'
      - 'BACKEND_API_URL=${_BACKEND_API_URL}'
      - '--memory'
      - '512Mi'

images:
  - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPOSITORY}/backend:${_TAG}'
  - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPOSITORY}/frontend:${_TAG}'

substitutions:
  _REGION: us-central1
  _REPOSITORY: land-guard
  _TAG: latest
  _FIREBASE_API_KEY: AIzaSyD-mgQuXtECcxCvt0N69_c246IAsZNXOXw
  _FIREBASE_AUTH_DOMAIN: trust-trailblazers.firebaseapp.com
  _FIREBASE_STORAGE_BUCKET: trust-trailblazers.firebasestorage.app
  _FIREBASE_MESSAGING_SENDER_ID: '593405835352'
  _FIREBASE_APP_ID: '1:593405835352:web:2e23ca4c05bc28405b722d'
  _BACKEND_API_URL: 'https://land-guard-api-593405835352.us-central1.run.app/api/v1'
  _FRONTEND_URL: 'https://land-guard-web-593405835352.us-central1.run.app'

options:
  dynamic_substitutions: true
